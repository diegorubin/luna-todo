.modal-dialog
  .modal-content
    .modal-header
      %button.close{"data-dismiss" => "modal", :type => "button"}
        %span{"aria-hidden" => "true"} ×
        %span.sr-only Close
      %h4#myModalLabel.modal-title= title 

    = form_for [:api, :v1, @list] do |f|
      .modal-body

        = f.label :title
        = f.text_field :title

      .modal-footer
        %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"}= t('form.buttons.close')
        %button.btn.btn-primary.submit-api-form{:type => "button"}= t('form.buttons.save')

